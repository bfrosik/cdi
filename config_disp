// GENERAL
save_dir = "results"
                             // directory where results of reconstructions are saved and the vtk files, default "results"

// PARAMETERS FOR VISUALISATION
// wavelength
lamda = .13933;
// delta (degrees)
delta = 30.1;
// gamma (degrees)
gamma = 14.0;
// camera distance (m)
arm=0.6350;
// angular step size
dth=0.000174532925199;

// detector pixel sixe
pixel = (55.0e-6, 55.0e-6);

// size to crop the image array
//crop = (120,120,120)
crop = (.75, .75, .75)

